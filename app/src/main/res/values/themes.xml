<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Getir" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/app_purple</item> <!-- Add this line -->
        <item name="android:windowBackground">@color/view_grey_color</item>
    </style>

    <style name="Theme.Getir" parent="Base.Theme.Getir" />
</resources>
